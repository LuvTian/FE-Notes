new Date()时间对象的参数可以是时间戳，也可以是一个日期时间，不填参数默认就是当前的时间，返回的结果是一个时间对象，打印出来是Tue Apr 23 2019 22:49:52 GMT+0800 (中国标准时间)这种，但是是一个对象，上面有许多方法可以使用

js转换成时间戳的最简单的方法:
+new Date() 用隐式类型转换 精确到毫秒级
Math.round(new Date())精确到毫秒级
Math.round(new Date()/1000)精确到秒级