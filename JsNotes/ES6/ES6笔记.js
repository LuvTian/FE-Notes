let map = new Map()
map.set('', '')

for(let [key,value] of map.entries()){ // map的键值对

}

map.has()
map.get()

arr.includes()
str.includes()


// &&是第一个条件为true才会执行第二个语句 if(true) ...

对象 delete 方法
if else 后面的语句只有一句的时候可以省略{} 

Object.keys(arr)

instanceof Array
